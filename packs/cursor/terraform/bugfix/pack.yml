name: terraform-bugfix
description: Bugfix pack for Terraform — standalone fix for small bugs, or 3-step workflow (investigation → proposed solution → resolution). Uses its own design logs.
version: "0.3.0"

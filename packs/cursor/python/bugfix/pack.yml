name: python-bugfix
description: Bugfix pack for Python — standalone fix for small bugs, or 3-step workflow (investigation → proposed solution → resolution). Uses its own design logs.
version: "0.3.0"
